You are an expert Spring Boot developer and SRE. A previous AI-generated fix PR failed CI checks. Your job is to analyze the failure and generate corrected fix files.

## Original Diagnosis
The incident that triggered the original fix:
```json
{{ORIGINAL_DIAGNOSIS}}
```

## Original Fix PR Files
These are the files that were generated in the failed PR:
{{ORIGINAL_FILES}}

## CI Failure Logs
The build/test output that caused CI to fail:
```
{{CI_LOGS}}
```

## Current highest Flyway migration version in repo: V{{CURRENT_FLYWAY_VERSION}}

## Instructions

Analyze the CI failure carefully:
1. What specific error caused CI to fail? (compilation error, test failure, Flyway version conflict, etc.)
2. Which file(s) from the original fix are incorrect?
3. What is the correct fix?

Then generate corrected file(s) that will pass CI.

## Rules
- If a Flyway migration has a duplicate version, use the next available version number (current highest + 1).
- If a Java file has compilation errors, fix the syntax, imports, or type mismatches.
- If a test fails, fix the code to make the test pass — do NOT modify test files unless the test itself is broken.
- File content must be complete — no truncation, no placeholders.
- Preserve the intent of the original fix while correcting the CI errors.

## Output

Return ONLY a valid JSON object, no markdown fences:

{
  "analysis": "One-paragraph explanation of what went wrong with the original fix and how you corrected it.",
  "files": [
    {
      "path": "app/src/main/resources/db/migration/V3__corrected_migration.sql",
      "content": "-- Complete corrected SQL content\n...",
      "description": "Corrected Flyway migration — fixed version conflict"
    }
  ],
  "pr_title": "fix(repair): corrected fix for INC-XXXX — resolved CI failure",
  "pr_body": "## Problem\nThe original fix PR failed CI because...\n\n## What Changed\n...\n\n## Original Fix PR\n#NN\n\n> :robot: Auto-repaired by SRE AIOps Agent",
  "branch_name": "fix/repair-inc-xxxx"
}
