# Alerting rules generated by SRE AIOps Agent
# Incident: INC-2024-002 — INC-2024-002
# Date: 2024-04-10

groups:
  - name: bookshop-srv-high-500-alert
    rules:
      - alert: BookshopHigh500Errors
        expr: rate(http_server_requests{status=~"5.."}[1m]) > 5
        for: 1m
        labels:
          service: bookshop-srv
          severity: P1
        annotations:
          summary: "HTTP 500 error spike detected on bookshop-srv"
          description: "High rate of HTTP 500 errors (> 5/min) on the Bookshop service's API endpoints. Immediate investigation required."
