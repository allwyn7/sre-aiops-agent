# Alerting rules generated by SRE AIOps Agent
# Incident: INC-2024-002 — INC-2024-002
# Date: 2024-04-10

groups:
  - name: bookshop-srv HTTP Alerts
    rules:
      - alert: HighHTTP500Rate
        expr: rate(http_requests_total{status=~"5..", app="bookshop", environment="production"}[5m]) > 5
        for: 1m
        labels:
          severity: page
          service: bookshop-srv
        annotations:
          summary: "Unusual rate of HTTP 500s in bookshop-srv production."
          description: "HTTP 500 rate exceeded threshold in the production environment for bookshop-srv. Investigate schema and API errors."
