# Runbook: Schema Drift: Missing price_old Column

> **Incident:** INC-2024-002 | **Service:** `bookshop-srv` | **Date:** 2024-04-10

**Trigger Pattern:** `SQLGrammarException: column \"price_old\" does not exist`

---

## 1. Detection

Prometheus HTTP 500 rate alerts and application logs showing SQLGrammarException

## 2. Triage

1. Check alerts for HTTP 5xx rate spikes.
2. Review logs for SQLGrammarException on `/api/books` endpoints.
3. Identify missing schema changes causing drift.

## 3. Diagnosis

1. Verify JPA entity mappings in `Book` class.
2. Check database schema for missing columns (`price_old`).
3. Inspect recent PRs affecting the `Book` entity.

## 4. Resolution

1. Write a Flyway migration to re-add the `price_old` column.
2. Commit the migration and deploy to affected environments.
3. Verify API stability and validate schema changes.

## 5. Verification

1. Confirm `/api/books` endpoints return 200 responses.
2. Use `\d+` PostgreSQL query to verify `price_old` exists.
3. Run integration tests for Book service.

## 6. Prevention

1. Add Flyway migrations for all database changes.
2. Implement a pre-deploy check for schema drift between JPA and database.

---

**Post-Incident Issue:** 
**Fix PR:** https://github.com/allwyn7/sre-aiops-agent/pull/7

_Generated by SRE AIOps Agent_
