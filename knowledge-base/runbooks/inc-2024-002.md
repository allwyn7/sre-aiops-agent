# Runbook: Resolving SQLGrammarException due to missing `price_old` column

> **Incident:** INC-2024-002 | **Service:** `bookshop-srv` | **Date:** 2024-04-10

**Trigger Pattern:** `SQLGrammarException: column \"price_old\" does not exist`

---

## 1. Detection

Alert from SAP BTP Application Logging for HTTP 500 spike; Logs showing SQLGrammarException when accessing `price_old` column.

## 2. Triage

1. Review error logs for SQLGrammarException related to `price_old`.
2. Check Flyway migration history to confirm status of `price_old` column.
3. Verify the Book entity references in `BookService` and `BookController`.

## 3. Diagnosis

1. Confirm if `price_old` column is present in the `Books` table in PostgreSQL.
2. Examine recent schema changes and code references to `price_old`.

## 4. Resolution

1. Restore the `price_old` column via a Flyway migration (V2 as created here).
2. Deploy the fix to production.
3. Verify the affected endpoints no longer throw SQLGrammarException.

## 5. Verification

Access `GET /api/books` endpoints and ensure 500 errors are resolved.

## 6. Prevention

Institute automated test case coverage to detect schema and code mismatches during CI validation.

---

**Post-Incident Issue:** 
**Fix PR:** https://github.com/allwyn7/sre-aiops-agent/pull/13

_Generated by SRE AIOps Agent_
