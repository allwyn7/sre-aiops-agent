# Runbook: Restore price_old column to resolve schema drift

> **Incident:** INC-2024-002 | **Service:** `bookshop-srv` | **Date:** 2024-04-10

**Trigger Pattern:** `org.hibernate.exception.SQLGrammarException: could not extract ResultSet\n.*ERROR: column \"price_old\" does not exist`

---

## 1. Detection

Prometheus alert monitoring for HTTP 500 error codes specifically on Book endpoints (`GET /api/books`, etc.) combined with log analysis for Hibernate SQLGrammarException involving `price_old`.

## 2. Triage

- Confirm high HTTP 500 error rate from alert details.
- Examine logs for `SQLGrammarException` and column-related errors like `price_old does not exist`.

## 3. Diagnosis

- Identify the missing column in the error logs.
- Check recent PRs for changes to the entity or database schema.
- Verify affected Flyway migration versions using database schema queries in the Flyway schema history table (`flyway_schema_history`).

## 4. Resolution

- Implement a Flyway migration script to add the missing `price_old` column back into the `book` table.
- Deploy to test or staging environments to ensure no additional regressions.
- Redeploy affected services in production.

## 5. Verification

- Verify the `price_old` column exists and is of type NUMERIC(19,2) using database schema inspection tools.
- Run test cases against `/api/books` endpoints, ensuring zero 500 errors.
- Check Flyway migration success logs post-deployment.

## 6. Prevention

- Ensure all schema changes in PRs are accompanied by matching Flyway migrations.
- Add CI checks to validate Flyway schema sync before merging schema-altering PRs.

---

**Post-Incident Issue:** 
**Fix PR:** https://github.com/allwyn7/sre-aiops-agent/pull/15

_Generated by SRE AIOps Agent_
