# SRE Recommendations: INC-2024-007

> **Service:** `bookshop-srv` | **Severity:** P1 | **Date:** 2024-04-28

---

## Capacity Planning

### Current Utilization
100% error rate on all HTTPS traffic to bookshop.example.com at the time of incident. 312 handshake failures per minute, 289 HTTP 4xx errors per minute. HTTP 2xx requests dropped to 0.

### Scaling Thresholds
None identified for this incident. See resource recommendations for increasing redundancy and capacity.

### Resource Recommendations
1. Increase ingress controller replicas to 3 for high availability. 2. Review HikariCP max pool size for bookshop-srv (recommended: `spring.datasource.hikari.maximum-pool-size=50` for PostgreSQL). 3. Add Horizontal Pod Autoscaler for bookshop-srv with target CPU utilization of 60%.

### Growth Projections
At 2x load: Increase ingress controller replicas to 5, increase HikariCP pool size to 100. At 5x load: Review node pool size, consider regional clusters. At 10x load: Reserve dedicated nodes for ingress pods, consider scaling to multiple regions, prepare for database sharding.

### Cost Considerations
Avoid over-provisioning of ingress controllers by implementing an HPA. Monitor traffic patterns to dynamically adjust resources. Evaluate higher-tier database plans if connection pooling is saturated.

---

## Disaster Recovery Assessment

### Single Points of Failure
- Ingress controller misconfiguration
- Single ACME challenge method (HTTP-01)
- Single ClusterIssuer (letsencrypt-prod)

### High Availability Recommendations
1. Increase replica count for ingress controllers to prevent single instance failure. 2. Configure DNS-01 challenge for cert-manager as a failover for HTTP-01. 3. Introduce a secondary ClusterIssuer for certificate redundancy (e.g., use different ACME providers).

### Failover Strategy
Switch to backup certificate issuers (e.g., fallback to self-signed using cert-manager's CA Issuer temporarily).

### RTO / RPO Assessment
RTO: <30 minutes (manual corrections needed upon failure). RPO: No data loss anticipated, ensure service availability.

### Backup Recommendations
Implement regular backups of cert-manager-related Kubernetes resources (such as Certificates and ClusterIssuers). Store TLS private keys securely off-cluster.

---

## Performance Tuning

### JVM Tuning
N/A — infrastructure incident

### Spring Boot Configuration
N/A — infrastructure incident

### JPA / Hibernate Tuning
N/A — infrastructure incident

### Database (PostgreSQL) Tuning
N/A — infrastructure incident

### Connection Pool (HikariCP) Tuning
N/A — infrastructure incident

### Kubernetes Tuning

1. Increase ingress-nginx replicas to 3 (ensure `spec.replicas: 3` in ingress-nginx deployment). 2. Enable PodDisruptionBudget to maintain ingress availability during node drain operations. Spec: `minAvailable: 2`. 3. Use affinity rules to spread ingress pods across multiple nodes.

### Network Tuning

Verify that HTTP-01 solver routes have proper ingress rules. Ensure DNS and FQDN for the challenge are reachable from the Let's Encrypt ACME server.

### Certificate Management

1. Use cert-manager readiness probes to verify solver ingress. 2. Implement alerting for certificates expiring within 7 days. 3. Automate fallback to a DNS-01 challenge if the HTTP-01 challenge fails repeatedly.

---

**Post-Incident Issue:** https://github.com/allwyn7/sre-aiops-agent/issues/32
**Fix PR:** https://github.com/allwyn7/sre-aiops-agent/pull/33

_Generated by SRE AIOps Agent_
