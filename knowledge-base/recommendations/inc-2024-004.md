# SRE Recommendations: INC-2024-004

> **Service:** `bookshop-srv` | **Severity:** P1 | **Date:** 2024-04-14

---

## Capacity Planning

### Current Utilization
100% of all requests failing due to XSUAA authentication mismatch. Requests dropped from 340 HTTP 2xx/min to 0, with 120 HTTP 401 errors/min and 45 HTTP 403 errors/min.

### Scaling Thresholds
Horizontal scaling: Trigger when CPU usage exceeds 70% for 5m (cpu_usage > 0.7).
Vertical scaling: Begin scaling at 80% memory (memory_utilization > 0.8).

### Resource Recommendations
1. Consider increasing JVM heap size to accommodate peak loads: Suggested value is `Xmx512m` (from default 256m).
2. HikariCP pool size: Increase to a max of 20 connections from current 10 to handle connection bursts.
3. Increase replica sets for bookshop-srv: Ensure starting with 3 pods, set HPA minReplicas=3, maxReplicas=10.

### Growth Projections
2x load: Event rates -> HTTP rate edge >80 connections possible.
5x growth within year boundary setup-> GPU instances pushing infra spends require analysis limits

### Cost Considerations
Expand efforts-based balancing & experiment projection sizing.

---

## Disaster Recovery Assessment

### Single Points of Failure
- XSUAA service binding credentials
- Hardcoded configuration (xsappname)

### High Availability Recommendations
1. Implement blue-green or canary deployments for changes to critical authentication and configuration settings.
2. Use dependency health checks for XSUAA during startup to validate configurations (audience, issuers).

### Failover Strategy
Implement dynamic fallback configurations or service registry information for XSUAA during rollout, allowing graceful fallback without total failures.

### RTO / RPO Assessment
RTO: Should be under 30 minutes upon accurate diagnosis and immediate action.
RPO: Ensure there are automated service binding snapshots taken during redeployment, as manual re-provisioning may introduce human error.

### Backup Recommendations
Automate periodic service binding backups and validations in CI pipeline to ensure recoveries are aligned with RPO.

---

## Performance Tuning

### JVM Tuning
Configure JVM flags: `-Xms512m -Xmx2g -XX:+UseG1GC -XX:InitiatingHeapOccupancyPercent=45`.

### Spring Boot Configuration
Ensure `server.tomcat.max-threads` set to `200`.
Update `spring.security.oauth.resourceserver.jwt.audience` to `bookshop-prod-eu20-v2`.

### JPA / Hibernate Tuning
Set `hibernate.jdbc.batch_size` to `30` for optimal parameterized batch inserts.
Enable `hibernate.order_inserts=true` and `hibernate.order_updates=true` for better write performance.

### Database (PostgreSQL) Tuning
Increase `max_connections` to 150 in PostgreSQL database.
Set `work_mem` to `16MB` for improved query performance with complex joins.

### Connection Pool (HikariCP) Tuning
Update HikariCP configuration:
* `maximumPoolSize: 20`
* `minimumIdle: 5`
* `idleTimeout: 30000ms`
* `leakDetectionThreshold: 5000ms`

---

**Post-Incident Issue:** https://github.com/allwyn7/sre-aiops-agent/issues/26
**Fix PR:** https://github.com/allwyn7/sre-aiops-agent/pull/27

_Generated by SRE AIOps Agent_
