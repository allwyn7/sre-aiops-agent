# SRE Recommendations: INC-2024-002

> **Service:** `bookshop-srv` | **Severity:** P1 | **Date:** 2024-04-10

---

## Capacity Planning

### Current Utilization
HTTP 5xx rate at 91/min compared to baseline of 0.2/min.

### Scaling Thresholds
Scale database or increase connection pool size if errors persist under high load.

### Resource Recommendations
Evaluate PostgreSQL query optimization for `Books` table.

### Growth Projections
Analyze current growth patterns of requests to `GET /api/books` and estimate future capacity.

### Cost Considerations
Consider costs for scaling database resources and optimizing queries.

---

## Disaster Recovery Assessment

### Single Points of Failure
- Schema mismatch between code and database.

### High Availability Recommendations
Introduce schema validation during application startup.

### Failover Strategy
Use feature toggles or fallbacks for non-essential fields like `price_old`.

### RTO / RPO Assessment
Target 99.9% availability for catalog services; ensure RTO < 5 mins for schema-related fixes.

### Backup Recommendations
Automate daily backups of PostgreSQL schema snapshots in production environment.

---

## Performance Tuning

### JVM Tuning
Verify JVM memory allocation and garbage collection during high traffic.

### Spring Boot Configuration
Ensure Spring Boot profiles are optimized for production.

### JPA / Hibernate Tuning
Enable Hibernate schema validation for development environments.

### Database (PostgreSQL) Tuning
Optimize PostgreSQL indexes for queries on `Books`.

### Connection Pool (HikariCP) Tuning
Configure HikariCP pool size to match concurrent endpoint traffic.

---

**Post-Incident Issue:** 
**Fix PR:** https://github.com/allwyn7/sre-aiops-agent/pull/13

_Generated by SRE AIOps Agent_
