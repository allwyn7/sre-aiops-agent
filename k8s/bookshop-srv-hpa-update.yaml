apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bookshop-srv
  namespace: bookshop-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bookshop-srv
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
